{% extends 'base.html.twig' %}

{% block title %}EauChaude{% endblock %}

{% block body %}
    <h1>EauChaude</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eau_chaude.id }}</td>
            </tr>
            <tr>
                <th>NomEauChaude</th>
                <td>{{ eau_chaude.nomEauChaude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eau_chaude_index') }}">back to list</a>

    <a href="{{ path('eau_chaude_edit', {'id': eau_chaude.id}) }}">edit</a>

    {{ include('eau_chaude/_delete_form.html.twig') }}
{% endblock %}
