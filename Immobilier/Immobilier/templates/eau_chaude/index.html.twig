{% extends 'base.html.twig' %}

{% block title %}EauChaude index{% endblock %}

{% block body %}
    <h1>EauChaude index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomEauChaude</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for eau_chaude in eau_chaudes %}
            <tr>
                <td>{{ eau_chaude.id }}</td>
                <td>{{ eau_chaude.nomEauChaude }}</td>
                <td>
                    <a href="{{ path('eau_chaude_show', {'id': eau_chaude.id}) }}">show</a>
                    <a href="{{ path('eau_chaude_edit', {'id': eau_chaude.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('eau_chaude_new') }}">Create new</a>
{% endblock %}
