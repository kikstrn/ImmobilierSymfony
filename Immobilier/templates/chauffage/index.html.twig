{% extends 'base.html.twig' %}

{% block title %}Chauffage index{% endblock %}

{% block body %}
    <h1>Chauffage index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomChauffage</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for chauffage in chauffages %}
            <tr>
                <td>{{ chauffage.id }}</td>
                <td>{{ chauffage.nomChauffage }}</td>
                <td>
                    <a href="{{ path('chauffage_show', {'id': chauffage.id}) }}">show</a>
                    <a href="{{ path('chauffage_edit', {'id': chauffage.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('chauffage_new') }}">Create new</a>
{% endblock %}
