{{ form_start(form) }}

    {{ form_widget(form) }}

<a href="{{ path('media_new') }}"><button type="submit" class="btn btn-warning">{{ button_label|default('Ajouter les médias') }}</button></a>


{{ form_end(form) }}

